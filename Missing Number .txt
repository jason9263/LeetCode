class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int sum = 0;
        int sizenums = nums.size();
        
        for(int i = 0 ;i < sizenums; i++){
            sum = sum + nums[i];
        }
        
        return sizenums*(sizenums + 1 )/2- sum;
    }
};
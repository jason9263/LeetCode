class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int protype = 0;
        int vsize = nums.size();
        for (int i = 0; i < vsize; i++){
            protype = protype ^ nums[i];
        }
        return protype;
    }
};